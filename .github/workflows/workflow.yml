
# ===============================
# ⚠️ UNSAFE WORKFLOW (for training purposes)
# ===============================
# This workflow runs automatically on push and executes a Python script
# that includes a nearly complete attack structure.
# NOTE: DO NOT USE IN PRODUCTION. EDUCATIONAL ONLY.

name: Unsafe Auto Trigger

on:
  push:  # ❗ Automatically runs when anyone pushes to the repository

jobs:
  simulate_attack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Unsafe Simulation (NEAR-REAL)
        run: |
          echo "Running UNSAFE simulation..."
          python unsafe_near_attack_example.py
